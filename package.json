{
  "name": "chrome-extension-elevenlabs-reader",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:env": "node scripts/build-env-config.js",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "playwright": "^1.56.1"
  }
}
